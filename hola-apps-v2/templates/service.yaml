apiVersion: v1
kind: Service
metadata:
  name: {{ include "hola-apps-v2.fullname" . }}
  labels:
    {{- include "hola-apps-v2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "hola-apps-v2.selectorLabels" . | nindent 4 }}
